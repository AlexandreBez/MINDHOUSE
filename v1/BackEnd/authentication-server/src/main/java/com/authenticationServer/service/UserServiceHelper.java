package com.authenticationServer.service;

import org.springframework.security.core.userdetails.UserDetailsService;

/**
 * The UserServiceHelper interface extends the UserDetailsService interface from Spring Security.
 * It defines the contract for a service class that handles user details and authentication in the application.
 */
public interface UserServiceHelper extends UserDetailsService {
	
}
